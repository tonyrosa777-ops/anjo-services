/**
 * Layout barrel — Anjo Services site chrome.
 *
 * Navigation: sticky fixed-top dark nav with hover-open dropdowns, "More"
 *   overflow, ⬥ Pricing amber marker, Phone + Text Tony + Take the Quiz cluster,
 *   full-screen mobile drawer with every nav item.
 * Footer: 4-column dark footer with services + service-areas + contact.
 */

export { default as Navigation } from "./Navigation";
export { default as Footer } from "./Footer";
